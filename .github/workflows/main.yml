name: Main
on:
  push:

env:
  SLACK_USERNAME: GithubActionBot
  SLACK_ICON: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png
  SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

#    - name: Setup Bun.sh
#      uses: oven-sh/setup-bun@v1
#
#    - name: Cache bun dependencies
#      uses: actions/cache@v2
#      with:
#        path: ~/.bun
#        key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lockb') }}
#        restore-keys: |
#          ${{ runner.os }}-bun-
#
#    - name: Install Dependencies
#      if: steps.bun-cache.outputs.cache-hit != 'true'
#      run: bun install

    - name: setup DB and Bun
      run: docker-compose up -d test db-test

    - name: Install Dependencies
      run: docker-compose run --rm test bun install

    - name: setup drizzle
      run: docker-compose run --rm test bun db:generate:test

    - name: Run unit test
      run: docker-compose run --rm test bun run test:ci

#    - name: Setup playwright
#      run: docker-compose run --rm test bun playwright install --with-deps
#
#    - name: Run Playwright tests
#      run: docker-compose run --rm test bun e2e:ci
#
#    - uses: actions/upload-artifact@v3
#      if: always()
#      with:
#        name: playwright-report
#        path: playwright-report/
#        retention-days: 30

    - name: Notify deployment
      uses: 8398a7/action-slack@v3.8.0
      with:
        status: ${{ job.status }}
        fields: all
      env:
        GITHUB_TOKEN: ${{ secrets.DEPENDABOT_AUTO_MERGE_TOKEN }}
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      if: always()
